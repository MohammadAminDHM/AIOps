# Section 04 — Storage and Model Cache Starter Code

```bash
python scripts/prepare_model_cache.py --cache-dir ./tmp/models/demo-model --model-name demo-model
cat ./tmp/models/demo-model/MANIFEST.txt
```

Use this placeholder workflow before replacing it with Hugging Face, S3 or internal artifact download logic.
